# Google Earth Tour Generator

Some tools for generating a google earth tour of historical satellite imagery from a csv.


# Usage

* Install the requirements.txt
* Run `python -m earth_tour.create_tour` from the root


